agent:
  system_template: |
    You are a helpful assistant that can interact with a computer.
    You handle both code changes and information tasks (questions, analysis, reports),
    and you always show the requested output / final answer to the user.
  instance_template: |
    {{task}}

    You can execute bash commands and edit files to accomplish the task.

    ## Task types

    - **Code changes** (fix a bug, implement a feature, refactor code, ...): edit the relevant files and verify your changes.
    - **Information tasks** (answer a question, analyze or report something, ...): investigate and present the result; you do not need to change any file.

    ## Recommended Workflow

    This workflow should be done step-by-step so that you can iterate on your changes and any possible problems.

    1. Decide whether the task needs code changes or only information.
    2. Analyze the relevant files or system state by finding and reading them.
    3. If code changes are needed: create a script to reproduce the issue, edit the source code to resolve it, and verify your fix works (also test edge cases).
    4. Otherwise: gather what you need and prepare a concise answer.
    5. Submit your final output and finish your work (see below).

    ## Submitting

    Finish by running exactly one command whose output starts with the line
    `COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`. Everything after that first line is saved and shown to
    the user, so always show the output the task asks for by including a concise final answer after it:
    for code changes a summary of what you changed, for information tasks the answer itself.
    Put the answer verbatim inside a quoted heredoc (replace the answer line below with your actual answer,
    across as many lines as you need) so that special characters such as `%`, `\` and `'` are not interpreted:

    ```bash
    cat <<'EOF'
    COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT
    Your final answer here
    EOF
    ```

    Do not run any other command after this one. <important>After this command, you cannot continue working on this task.</important>

    ## Command Execution Rules

    You are operating in an environment where

    1. You issue at least one command
    2. The system executes the command(s) in a subshell
    3. You see the result(s)
    4. You write your next command(s)

    Each response should include:

    1. **Reasoning text** where you explain your analysis and plan
    2. At least one tool call with your command

    **CRITICAL REQUIREMENTS:**

    - Your response SHOULD include reasoning text explaining what you're doing
    - Your response MUST include AT LEAST ONE bash tool call
    - Directory or environment variable changes are not persistent. Every action is executed in a new subshell.
    - However, you can prefix any action with `MY_ENV_VAR=MY_VALUE cd /path/to/working/dir && ...` or write/load environment variables from files
    - Submit your final output and finish your work by running the submission command described above
      (include your answer after the `COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT` line).
      <important>After this command, you cannot continue working on this task.</important>

    Example of a CORRECT response:
    <example_response>
    I need to understand the structure of the repository first. Let me check what files are in the current directory to get a better understanding of the codebase.

    [Makes bash tool call with {"command": "ls -la"} as arguments]
    </example_response>

    <system_information>
    {{system}} {{release}} {{version}} {{machine}}
    </system_information>

    ## Useful command examples

    ### Create a new file:

    ```bash
    cat <<'EOF' > newfile.py
    import numpy as np
    hello = "world"
    print(hello)
    EOF
    ```

    ### Edit files with sed:

    {%- if system == "Darwin" -%}
    <important>
    You are on MacOS. For all the below examples, you need to use `sed -i ''` instead of `sed -i`.
    </important>
    {%- endif -%}

    ```bash
    # Replace all occurrences
    sed -i 's/old_string/new_string/g' filename.py

    # Replace only first occurrence
    sed -i 's/old_string/new_string/' filename.py

    # Replace first occurrence on line 1
    sed -i '1s/old_string/new_string/' filename.py

    # Replace all occurrences in lines 1-10
    sed -i '1,10s/old_string/new_string/g' filename.py
    ```

    ### View file content:

    ```bash
    # View specific lines with numbers
    nl -ba filename.py | sed -n '10,20p'
    ```

    ### Any other command you want to run

    ```bash
    anything
    ```
  step_limit: 0
  mode: confirm
environment:
  env:
    PAGER: cat
    MANPAGER: cat
    LESS: -R
    PIP_PROGRESS_BAR: 'off'
    TQDM_DISABLE: '1'
model:
  observation_template: |
    {%- if output.output | length < 10000 -%}
    {
      "returncode": {{ output.returncode }},
      "output": {{ output.output | tojson }}
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- else -%}
    {
      "returncode": {{ output.returncode }},
      "output_head": {{ output.output[:5000] | tojson }},
      "output_tail": {{ output.output[-5000:] | tojson }},
      "elided_chars": {{ output.output | length - 10000 }},
      "warning": "Output too long."
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- endif -%}
  format_error_template: |
    {% if finish_reason is defined and (finish_reason == "length" or (finish_reason == "tool_calls" and not has_tool_calls)) -%}
    Your previous response reached the output token limit (finish_reason={{ finish_reason }}) before you produced a tool call, so it was cut off. Respond more concisely and finish with exactly one bash tool call. If you need to think more, do so briefly.
    {%- else -%}
    Tool call error:

    <error>
    {{error}}
    </error>

    Here is general guidance on how to submit correct toolcalls:

    Every response needs to use the 'bash' tool at least once to execute commands.

    Call the bash tool with your command as the argument:
    - Tool: bash
    - Arguments: {"command": "your_command_here"}

    If you want to end the task, run one command that outputs `COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT` as its
    first line, followed by your final answer/output.
    {%- endif %}
  model_kwargs:
    drop_params: true
